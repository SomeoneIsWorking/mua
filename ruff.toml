line-length = 100

[lint]
# Defect-oriented checks only; formatting and import-order churn are handled by
# the language-specific format gates where they matter.
select = ["F", "E4", "E7", "E9", "B", "PLE", "PLR0124", "RUF"]
